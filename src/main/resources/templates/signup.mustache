<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="RU-ru">
<head>
    <title>Spring Security Example </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
          integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

</head>
<body>
<div class="container">
    <h1 class="display-1">Регистрация</h1>
    <p style="color: red">
        {{#message}}
            {{message}}
        {{/message}}
    </p>
    <div class="input-group mb-3 justify-content-center">
        <form action="/signup" method="post">
            <div class="form-row">
                <label for="username">Логин</label>
                <input class="form-control {{#login_validation}}{{login_validation}}{{/login_validation}}" type="text"
                       name="username" id="username" value="{{#name}}{{name}}{{/name}}" required/>
                <div class="invalid-feedback">
                    {{#login_invalid_message}}
                        {{login_invalid_message}}
                    {{/login_invalid_message}}
                </div>
                <div class="valid-feedback">
                    OK
                </div>
            </div>
            <div class="form-row">
                <label for="password">Пароль</label>
                <input class="form-control {{#password_validation}}{{password_validation}}{{/password_validation}}"
                       type="password" name="password" id="password" value="{{#pass}}{{pass}}{{/pass}}"
                       required/>
                <div class="invalid-feedback">
                    {{#password_invalid_message}}
                        {{password_invalid_message}}
                    {{/password_invalid_message}}
                </div>
                <div class="valid-feedback">
                    OK
                </div>
            </div>
            <div><input type="submit" class="btn btn-light btn-block" value="Зарегистрироваться"/></div>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">

            <div><a href="/login">У меня есть уже есть учетная запись</a></div>
        </form>
    </div>
</div>
</body>
</html>